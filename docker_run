#!/usr/bin/env bash
docker run \
    --name z2m_dining \
    --restart=unless-stopped \
    -p 8080:8080 \
    -v ./data:/app/data \
    -v /run/udev:/run/udev:ro \
    -e TZ=America/Chicago \
	-d \
    koenkk/zigbee2mqtt
